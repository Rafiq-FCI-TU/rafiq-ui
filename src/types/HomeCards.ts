export interface Feature {
  img: string;
  title: string;
  description: string;
  bg: string;
}
export interface Care {
  number: number;
  title: string;
  description: string;
}
export interface Activity {
  img: string;
  title: string;
  description: string;
}